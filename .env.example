# These are placeholders only. Nothing in the app reads these yet —
# auth, product, and payment data are all mocked/local in this phase.
# Phase 2 wires these up to real AWS services.

# AWS Cognito (auth) — Phase 2
NEXT_PUBLIC_COGNITO_USER_POOL_ID=
NEXT_PUBLIC_COGNITO_CLIENT_ID=
NEXT_PUBLIC_COGNITO_REGION=

# API Gateway base URL — Phase 2
NEXT_PUBLIC_API_BASE_URL=

# Stripe — Phase 2
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=
